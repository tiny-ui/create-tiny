{
	"name": "create-tiny",
	"version": "0.1.2",
	"bin": {
		"create-tiny": "out.js"
	},
	"engines": {
		"node": "^14.18.0 || >=16.0.0"
	},
	"scripts": {
		"build": "esbuild index.ts --bundle --outfile=out.js",
		"dev": "npm run build && node out.js"
	},
	"devDependencies": {
		"typescript": "^4.8.4",
		"esbuild": "^0.15.10"
	}
}
