{
  "scripts": {
    "build": "esbuild src/index.js --bundle --outdir=out",
    "dev": "node esbuild.config.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.15.10",
    "@types/node": "^18.8.3"
  }
}
